

<h1 class="mt-5">顧客情報一覧</h1>
<div class="mt-2">
<table class="table table-hover">
  <thead>
    <tr>
      <th scope="col">名前</th>
      <th scope="col">性別</th>
      <th scope="col">メールアドレス</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th> <%= @user.name %></th>
      <td><%= @user.gender %></td>
      <td><%= @user.email %>円</td>
<!--       <td><%= @user.addresses.first.ship %></td>
 -->    </tr>

  </tbody>
</table>
<div class="text-right">
<%= link_to "プロフィール編集", edit_user_path(current_user.id) %>
</div>
<div class="text-right">
	<%=link_to "戻る", admintop_path ,class: "back"%>
</div>
</div>

